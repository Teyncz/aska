{% extends "base_panel.html.twig" %}

{% block body %}

    <h1>Bonjour, {{user.firstname|capitalize}}</h1>
    
    <aside>
    <ul>
        {% for domain in domains %}
            <li><a href="http://{{domain.name}}">{{domain.name}}</a></li>
        {% endfor %}

    </ul>
    </aside>

{% endblock %}